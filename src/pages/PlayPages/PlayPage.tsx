import Layout from '../../layouts/Layout.tsx';
import { FC } from 'react';

const PlayPage: FC = () => {
  return (
    <Layout>
      <main>Play</main>
    </Layout>
  );
};

export default PlayPage;
